interface:
  display_name: "JP Template Workspace Changes"
  short_description: "Keep JP workspace changes aligned"
  default_prompt: "Use $jp-template-workspace-changes to add, remove, or rename workspace packages without breaking manifests."
