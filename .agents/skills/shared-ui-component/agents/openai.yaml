interface:
  display_name: "Shared UI Component"
  short_description: "Build shared UI components correctly"
  default_prompt: "Use $shared-ui-component to add a reusable React component (published as @jainparichay/ui) or an app-local component in apps/web."
