{
  "name": "@workspace-starter/web-host",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "pnpm build && node dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "pnpm build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@workspace-starter/api": "workspace:*",
    "@workspace-starter/web": "workspace:*",
    "fastify": "^5.8.5",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workspace-starter/config-typescript": "workspace:*",
    "eslint": "catalog:dev-tools",
    "typescript": "catalog:"
  }
}
